---
"smooblue-app": patch
---

Notifications now show your like/repost state on reply, mention, and quote cards. The subject post was hydrated once and cached forever, so liking a reply elsewhere (or after a restart) never reflected — refresh polls now re-hydrate the page so viewer state stays current.
