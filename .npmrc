# pnpm 10 toggled `inject-workspace-packages` defaults across point
# releases — pinning explicitly so the lockfile's `settings:` block
# matches what `pnpm install --frozen-lockfile` sees in CI. Without
# this, CI fails with ERR_PNPM_LOCKFILE_CONFIG_MISMATCH.
inject-workspace-packages=true
